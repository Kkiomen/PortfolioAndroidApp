<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.navigationdrawerapapplication">

    <uses-sdk tools:overrideLibrary="com.pdfview.pdf" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationDrawerApApplication">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity2"
            android:label="@string/title_activity_maps2"></activity>
        <activity
            android:name=".GalleryViewActivity"
            android:label="@string/title_activity_gallery_view"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity
            android:name=".CurrentImageActivity"
            android:label="@string/title_activity_current_image"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity
            android:name=".GalleryActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RulesActivity"
            android:label="@string/title_activity_rules"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity
            android:name=".AuthorActivity"
            android:label="@string/title_activity_author"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity
            android:name=".ShopActivity"
            android:label="@string/title_activity_shop"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity
            android:name=".PortfolioActivity"
            android:label="@string/title_activity_portfolio"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity
            android:name=".ConcertPageActivity"
            android:label="@string/title_activity_concert_page"
            android:parentActivityName=".ConcertActivity"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity android:name=".ConcertPage" />
        <activity
            android:name=".ConcertActivity"
            android:label="@string/title_activity_concert"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.NavigationDrawerApApplication.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>